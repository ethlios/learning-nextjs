import * as React from 'react';

export interface PostPageProps {}

export default function PostPage(props: PostPageProps) {
    return (
        <div className="flex justify-center h-full">
            <h1 className="font-bold">Post Page</h1>
        </div>
    );
}
